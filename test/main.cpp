#include <iostream>
#include <cmath>
#include <cstdio>
using namespace std;
int f[60];
int t;
int main()
{
    cin >> t;
    while (t--){
        cin >> n;
        for (int i = 0;i <= n / 3;i++)
            for (int j = 0;j <= (n - i * 3) / 2;j++)
                ans +=
    }
    return 0;
}
